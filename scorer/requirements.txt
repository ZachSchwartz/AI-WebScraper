# Queue
redis==8.1.0

# Machine Learning
# No GPU is exposed to this container, so pull the CPU-only wheels: the default
# PyPI torch bundles ~2GB of CUDA libraries that can never be used here.
--extra-index-url https://download.pytorch.org/whl/cpu
numpy==2.5.2
torch==2.13.0+cpu
transformers==5.16.1
sentence-transformers==6.0.0

# Web Framework
flask==3.1.3
werkzeug==3.1.8

# Utilities
requests==2.34.2

# WSGI server
gunicorn==26.2.0
