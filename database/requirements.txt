# Pinned to the same versions requirements-dev.txt installs, so the suite that
# gates a merge is running the code these images actually ship.

# Database service requirements
redis==5.0.1
sqlalchemy==2.0.44
psycopg2-binary==2.9.9  # PostgreSQL adapter

# Web Framework
flask==3.1.3
werkzeug==3.1.8

# Utilities
requests==2.34.2
