# Test and lint dependencies shared by every service.
# torch and sentence-transformers are deliberately absent; see tests/conftest.py.
pytest==9.1.1
pytest-cov==7.1.0
beautifulsoup4==4.15.0
requests==2.34.2
flask==3.1.3
werkzeug==3.1.8
numpy==2.5.2
sqlalchemy==2.0.52
redis==8.1.0
fakeredis==2.37.1  # In-process Redis so the queue tests need no server.
black==26.5.1
pylint==4.0.7
mypy==2.3.1
